Register yourself as "orchestrator" in the global roles map. Spawn a worker that registers itself as "worker" in roles, looks up the orchestrator's handle from globals, and sends it the number 42. Return what you receive.
