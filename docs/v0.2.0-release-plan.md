# Spell v0.2.0 Release Checklist

**Status:** final release-preparation checklist
**Release intent:** first public-facing software release of Spell.
**Target integration branch:** `v0.2.0-dev`

Spell v0.2.0 should present the repository as an experimental,
CLI-first implementation of self-programmed execution. The release assumes
a paid or authenticated LLM provider for normal use; the no-cost `--test`
provider remains an installation smoke test. v0.1.0 remains the
paper/reproducibility-oriented release, while v0.2.0 is the first release
focused on making the repository readable, presentable, and usable as
software.

API design, Python adapter design, and API documentation are intentionally
tracked separately from this checklist.

## Integration Status

- [x] Work Package 1: legal and release metadata.
  - Added `LICENSE`.
  - Added `CHANGELOG.md`.
  - Checked README/license/changelog consistency without adding package
    manager, Docker, Homebrew, or Python packaging claims.

- [ ] Work Package 2: human-facing conceptual documentation.
  - Tracked by PR #113, `codex/v020-language-overview` into `v0.2.0-dev`.
  - Adds `docs/language-overview.md` and links it from public source
    guidance.
  - Still separate while PR #113 remains open.

- [x] Work Package 3: public source and configuration guides.
  - Cleaned `AGENTS.public.md`.
  - Kept and public-cleaned `config/AGENTS.md`, `examples/AGENTS.md`, and
    `test/AGENTS.md`.
  - Removed tracked private/local guidance files from the public release
    surface and added ignore rules for local copies.

- [x] Work Package 4: examples and public onboarding docs.
  - Curated the public example set to `hello-world`, `coin-flip`,
    `twenty-questions`, `telephone`, `auction`, and `chat`.
  - Removed non-blessed examples and stale companion docs.
  - Normalized example provider/setup wording toward live provider use.

- [x] Work Package 5: public release-surface cleanup and verification.
  - Removed accidental tracked notebook material.
  - Removed the tracked benchmark catalog from the public archive surface.
  - Removed internal GCP and paper benchmark operational scripts.
  - Removed tracked Claude-local helper files from the public archive surface.
  - Removed the shell test that only covered the removed GCP launcher.
  - Kept runtime benchmark API code and Clojure tests in scope for API work.
  - Updated `.gitignore` so local Claude workspace files stay out of release
    commits.

## Separate API Work

The public API/configuration migration remains outside this checklist and is
being handled independently. Do not fold API-branch decisions, Python adapter
changes, or API documentation rewrites into release-surface cleanup PRs unless
explicitly requested.

## Final Release Checks

Before cutting or merging the integrated v0.2.0 release branch:

- [ ] Merge or explicitly defer PR #113.
- [ ] Merge or explicitly defer the separate API/configuration work.
- [ ] Run CLI smoke checks:
  - `bin/spell -h`
  - `bin/spell -t "Return a short greeting"`
- [ ] Run fast tests:
  - `clj -M:test-fast`
- [ ] Check patch hygiene:
  - `git diff --check`
- [ ] Inspect the tracked archive surface:
  - `git archive --format=tar HEAD | tar -tf - | sort`
- [ ] Confirm the archive contains no tracked `notebook/` files.
- [ ] Confirm the removed GCP and paper benchmark scripts are absent.
- [ ] Search public docs and examples for stale private/research wording:
  - `rg -n "benchmark|paper|reviewer|notebook|GCP|CLAUDE|NeurIPS|reproduc" README.md AGENTS.public.md docs examples config test`

Intentional remaining hits should be limited to public-facing references such
as the README paper link, v0.1.0 reproducibility notes, API docs' benchmark
adapter wording while API work is separate, and test/source names where the
runtime code remains intentionally in scope.
