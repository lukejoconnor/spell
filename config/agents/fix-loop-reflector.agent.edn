;; Diagnostic agent for fix-loop pattern.
;; Analyzes test output and diffs, returns structured diagnosis.
;; Format validation ensures :diagnosis, :keep-changes, :panic keys.
;; io namespace available for reading source files (read-only by instruction).

{:base base-tc.agent.edn
 :name fix-loop-reflector
 :doc "Diagnostic agent for fix-loop pattern. Analyzes test failures and diffs."
 :format {:required [:diagnosis :keep-changes :panic]}
 :namespaces {io stdlib/io}
 :llms []}
