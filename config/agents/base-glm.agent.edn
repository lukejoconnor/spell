;; Base agent for GLM models via Fireworks (prefill mode).
;; Uses GLM-specific system prompt with extra wrapper-closing antipatterns.

{:name base-glm
 :doc "Base agent for GLM models (prefill, GLM-specific prompt)"
 :system {:file "../prompts/minimal-glm.txt"}
 :llms []}
