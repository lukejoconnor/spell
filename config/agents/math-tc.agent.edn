;; Math benchmark agent for tool-call transport. No web namespace —
;; web search wastes tokens on competition math (see TODO #152).

{:base base-tc.agent.edn
 :name math-tc
 :doc "Tool-call transport math agent with io namespace only (no web)"

 :namespaces
 {io stdlib/io}
 :llms []}
