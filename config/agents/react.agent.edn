;; Hidden ReAct loop profile.
;; The inner model runs only through leaf-llm; this agent exists to expose
;; react/ plus the minimal shell execution capability needed by the loop.

{:base base-tc.agent.edn
 :agent-name react
 :agent-description "Hidden ReAct loop profile"
 :namespaces
 {io    stdlib/io-exec
  react stdlib/react}}
