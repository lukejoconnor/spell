;; Math benchmark agent for message transport. No web namespace —
;; web search wastes tokens on competition math (see TODO #152).

{:base base-msg.agent.edn
 :name math-msg
 :doc "Message transport math agent with io namespace only (no web)"

 :namespaces
 {io stdlib/io}
 :llms []}
