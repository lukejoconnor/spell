;; Math benchmark agent for prefill transport. No web namespace —
;; web search wastes tokens on competition math (see TODO #152).

{:base base-pf.agent.edn
 :name math-pf
 :doc "Prefill transport math agent with io namespace only (no web)"

 :namespaces
 {io stdlib/io}
 :llms []}
