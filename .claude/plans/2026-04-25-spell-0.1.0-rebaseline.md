# Spell 0.1.0 Rebaseline And 0.1.2 Seed Branch

**Date:** 2026-04-25
**Mode:** `$ship` standard mode; plan first, wait for approval before implementation.

## Goal

Reorganize Spell branch history after the 0.1.1 regression investigation:

- Keep the nested `benchmarking/` repository untouched so its harness-side fixes remain in place.
- Make Spell `main` the 0.1.0 manuscript reference.
- Preserve the current 0.1.1 state on an explicit archival branch.
- Create a future 0.1.2 branch based on 0.1.0 plus only the selected runtime/API fixes:
  - trailing-expression/error recovery changes
  - missing-tool-call retry wording
  - absolute default trace directory
  - timeout/killed-run reporting
- Add a notebook TODO to prune the archived 0.1.1 branch later.

## Current State

- Current worktree: `/Users/loconnor/Dropbox/GitHub/spell`
- Current branch: `prompt-011-pilot`
- Current HEAD: `c33b7d001e1b9a7eeced7ab2ed18255da654aba0`
- `main`: `8e2bda9dd25aea84e1bec9679634e47a03d7dac8` (`Merge prompt 0.1.1 and GCP benchmark fixes`)
- `v0.1.0`: annotated tag object `3134a24d3ef93cfa0338aaa956d79bfad80d321e`, peeled commit `5ad5c57d0d9ae39d53acd0ad6a35e2e71c0a60c0`
- `main` is currently checked out in `.claude/worktrees/todo-260-trace-dir-clean`.
- Dirty main-repo state in the current worktree is only untracked:
  - `.claude/plans/`
  - `workspace/`
- The `main` worktree is clean.

The current environment is `workspace-write` with restricted network. This workflow is more reliable with full permissions because it creates worktrees, pushes branches, and opens a PR, but the local planning and implementation can start as-is. Remote push/PR steps may need approval or credentials.

## Branch Plan

1. Preserve 0.1.1 before moving `main`.
   - Create an explicit branch such as `archive/spell-0.1.1-main-2026-04-25` at `8e2bda9`.
   - Leave the existing `prompt-011-pilot` branch at `c33b7d0` intact as the current pilot checkout.
   - If desired for extra clarity, also create `archive/spell-0.1.1-current-2026-04-25` at `c33b7d0`.

2. Rebaseline `main`.
   - Preferred interpretation of "exact 0.1.0 checkout": move `main` itself to `v0.1.0` after the archival branch exists.
   - This is a branch rewrite, not a normal PR merge. If pushed to `origin/main`, it should be done with `--force-with-lease`.
   - Validation target: `git rev-parse main` equals `5ad5c57d0d9ae39d53acd0ad6a35e2e71c0a60c0`, and `git diff --exit-code v0.1.0 main` is clean.

3. Create future 0.1.2 branch.
   - Branch name: `codex/spell-0.1.2-recovery-trace-timeouts`.
   - Base: `v0.1.0`.
   - Cherry-pick or manually replay only these commits/patches:
     - `7d5b305` - missing tool-call retry prompt wording.
     - `8a9e2f3`, `ad6781d`, `02a5129` - trailing-expression/same-tail recovery.
     - `768b44f`, `139f100` - timeout/killed-run benchmark API reporting.
     - `be5a662` - absolute default trace directory.
   - Do not include prompt 0.1.1 surface changes:
     - sysprompt rewrites
     - `:coding` reminder rewrite
     - `:swe-bench` removal
     - `patterns` removal from `io-tc`
     - stronger orchestrator wording
   - Do not include unrelated provider/pricing/GCP launcher changes unless needed to resolve a direct compile/test conflict.

4. Open a PR for the 0.1.2 seed branch against the rebaselined `main`.
   - The PR is for review and future work, not for immediate merge, because `main` should remain the manuscript reference.

## Expected Files In 0.1.2 Branch

Likely touched by the selected changes:

- `spell_benchmark_client.py`
- `src/spell/api.clj`
- `src/spell/benchmark_api.clj`
- `src/spell/cli.clj`
- `src/spell/eval.clj`
- `src/spell/llm.clj`
- `src/spell/recovery.clj`
- `src/spell/trace.clj`
- `README.md`
- `test/spell/api_test.clj`
- `test/spell/benchmark_api_test.clj`
- `test/spell/llm_test.clj`

Likely unchanged from 0.1.0 on `main`, and intentionally not carried into 0.1.2 unless conflicts force a tiny support edit:

- `config/prompts/sysprompt-*.txt`
- `config/agents/io-tc.agent.edn`
- `src/spell/stdlib.clj`
- `src/spell/macros.clj`
- `src/spell/provider.clj`
- `data/pricing.edn`
- `scripts/gcp-benchmark.sh`
- `scripts/gcp-startup.sh`

The nested `benchmarking/` repository should not be edited, reset, or committed.

## Notebook And TODO

This is substantial repo-state work, so create a notebook entry recording:

- old `main` commit
- `v0.1.0` commit
- archive branch name(s)
- 0.1.2 seed branch name
- exact commits/patches included in 0.1.2
- validation results

Also add a TODO item:

- "Prune archived 0.1.1 branch(es) after the manuscript reference and 0.1.2 path settle."

Use the next TODO ID from `notebook/TODO.md` at implementation time and commit notebook changes in the separate notebook repository.

## Validation

Before branch manipulation:

- `git status --porcelain=v1 -b`
- `git -C .claude/worktrees/todo-260-trace-dir-clean status --porcelain=v1 -b`
- `git -C benchmarking status --porcelain=v1 -b`

For `main` rebaseline:

- `git rev-parse main v0.1.0^{commit}`
- `git diff --exit-code v0.1.0 main`

For the 0.1.2 seed branch:

- `git diff --name-status v0.1.0..codex/spell-0.1.2-recovery-trace-timeouts`
- `clojure -M:test -n spell.llm-test -n spell.benchmark-api-test -n spell.api-test`
- `clojure -M:test` if focused tests pass and runtime allows.

For nested benchmarking:

- `git -C benchmarking status --porcelain=v1 -b`
- Confirm no `benchmarking/` files changed.

For remote branch/PR work:

- Push archive branch(es).
- Push `main` with `--force-with-lease` only after explicit approval.
- Push `codex/spell-0.1.2-recovery-trace-timeouts`.
- Open a PR for the 0.1.2 seed branch, clearly marked "do not merge until ready."

## Review Plan

After implementation opens the 0.1.2 PR, run two fresh-context reviews:

- General correctness review: branch topology, included/excluded commits, compile/test issues.
- Regression/test-coverage review: verify the selected fixes are present, prompt 0.1.1 changes are absent, and `benchmarking/` remained untouched.

Patch straightforward findings in the PR branch. Do not merge the PR unless separately requested.

## Risks And Decisions

- Moving `main` to `v0.1.0` is a history rewrite if pushed to `origin/main`. The archive branch must exist first, and the remote update should use `--force-with-lease`.
- A non-history-rewrite alternative is to add a revert commit whose tree equals `v0.1.0`. That is safer for shared remotes but less literally an "exact 0.1.0 checkout." This plan follows the user's literal request.
- The selected 0.1.2 commits may have minor conflicts because they were developed after provider/GCP/prompt work. Resolve conflicts narrowly and do not pull in unrelated 0.1.1 prompt/provider changes.
- Existing stale/prunable worktrees should not be cleaned up as part of this task unless they block branch operations.
